<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_location_security_users_form" model="ir.ui.view">
        <field name="name">location_security.users.form</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <group name="messaging" position="before">

            </group>
            <notebook>
                <page string="Allowed Warehouse and Journals">
<!--                    <group string="Locations">-->
<!--                        <field name="restrict_locations"/>-->
<!--                    </group>-->
                    <group string="Allowed Stock OPeration Type">
                        <field name="default_picking_type_ids" nolabel="1" colspan="2" widget="many2many_tags"/>
                    </group>
                    <group string="Allowed Stock Locations(Destination)">
                        <field name="stock_location_ids" nolabel="1" colspan="2"  widget="many2many_tags"/>
                    </group>
                    <group string="Allowed Journals">
                        <field name="account_journal_ids" nolabel="1" colspan="2"  widget="many2many_tags"/>
                    </group>
                </page>
            </notebook>
        </field>
    </record>
</odoo>
